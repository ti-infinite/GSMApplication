# ── Dependencias
/node_modules

# ── Vite build output
/dist

# ── Variables de entorno (nunca commitear .env.local)
.env.local
.env.*.local

# ── Misc
.DS_Store
*.pem
Thumbs.db

# ── Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ── TypeScript incremental
*.tsbuildinfo

# ── Caché de herramientas
.turbo/
.cache/

# ── Orval generated API clients (source of truth = swagger/)
src/shared/api/