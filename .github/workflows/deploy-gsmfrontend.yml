name: Deploy GSMFrontend
on:
  push:
    branches: ["develop"]
    paths:
      - "gsmapplications-frontend/**"
  pull_request:
    branches: ["develop", "main"]
    types: [closed]
    paths:
      - "gsmapplications-frontend/**"
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment (dev/qa/prod)'
        default: 'dev'

env:
  AWS_REGION: us-east-1

jobs:
  determine-env:
    if: ${{ vars.WORKFLOW_FRONTEND_ENABLED == 'true' }}
    name: Determine Environment
    runs-on: ubuntu-latest
    outputs:
      environment: ${{ steps.set-env.outputs.environment }}
    steps:
      - name: Set Environment
        id: set-env
        run: |
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            ENV="${{ github.event.inputs.environment }}"
          else
            BRANCH="${{ github.ref_name }}"
            if [ "$BRANCH" = "main" ]; then
              ENV="prod"
            elif [ "$BRANCH" = "quality" ]; then
              ENV="qa"
            elif [ "$BRANCH" = "develop" ]; then
              ENV="dev"
            else
              ENV="dev"
            fi
          fi
          echo "environment=$ENV" >> $GITHUB_OUTPUT
          echo "Deploying gsmapplication-$ENV-frontend"

  deploy:
    if: ${{ vars.WORKFLOW_FRONTEND_ENABLED == 'true' && (github.event_name != 'pull_request' || github.event.pull_request.merged == true) }}
    name: Deploy GSMFrontend
    needs: determine-env
    runs-on: ubuntu-latest
    environment: frontend-${{ needs.determine-env.outputs.environment }}
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: gsmapplications-frontend/package-lock.json

      - name: Install dependencies
        working-directory: gsmapplications-frontend
        run: npm ci

      - name: Generate API clients
        working-directory: gsmapplications-frontend
        run: npm run generate

      - name: Build
        working-directory: gsmapplications-frontend
        env:
          VITE_TENANT_DEFAULT: ${{ vars.VITE_TENANT_DEFAULT }}
          VITE_TENANT_IDS: ${{ vars.VITE_TENANT_IDS }}
        run: npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_INFRA_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Sync build to S3
        working-directory: gsmapplications-frontend
        run: |
          aws s3 sync ./dist/ s3://${{ vars.S3_BUCKET_NAME }}/admin/ \
            --delete \
            --checksum-algorithm SHA256

      - name: Wait for CloudFront distribution
        run: |
          aws cloudfront wait distribution-deployed \
            --id ${{ vars.CLOUDFRONT_DISTRIBUTION_ID }}

      - name: Invalidate CloudFront cache
        run: |
          aws cloudfront create-invalidation \
            --distribution-id ${{ vars.CLOUDFRONT_DISTRIBUTION_ID }} \
            --paths "/*"
